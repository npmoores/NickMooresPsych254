stat_smooth(method="glm", family="binomial", se=FALSE)
ds <- d.all.tidy %>% filter((d.all.tidy$subjective.test==1) & (d.all.tidy$objective.test >= .6))
ds <- tbl_df(ds)
plot.style <- theme(panel.grid.major=element_blank(), legend.position="right", plot.title=element_text(face="bold", size=14), axis.title=element_text(size=12))
PD <- position_dodge(.9)
ds_graph <- ds
ds_graph <- select(ds_graph, one_of(c("subjectID","presentation.time","congruent","RT")))
ds_graph
glimpse(graph_df)
glimpse(ds_graph)
ds_graph <- ds_graph %>% spread("congruent","RT")
ds_graph
View(ds_graph)
ds_graph2 <- ds_graph %>% spread(congruent:RT)
?spread
ds_graph2 <- ds_graph %>% spread(congruent,RT)
ds_graphcon <- ds_graph %>% filter(ds_graph$congruent=="yes")
congruent_rts <- aggregate(RT ~ subjectID, ds_graphcon, mean)
congruent_rts
ds_graphinc <- ds_graph %>% filter(ds_graph$congruent=="no")
incongruent_rts <- aggregate(RT ~ subjectID, ds_graphcon, mean)
incongruent_rts
ds_graphinc <- ds_graph %>% filter(ds_graph$congruent=="no")
incongruent_rts <- aggregate(RT ~ subjectID, ds_graphinc, mean)
incongruent_rts
ds_graph <- ds
ds_graph <- select(ds_graph, one_of(c("subjectID","presentation.time","congruent","RT")))
ds_graphcon <- ds_graph %>% filter(ds_graph$congruent=="yes") %>% rename(yesRT=RT)
congruent_rts <- aggregate(yesRT ~ subjectID, ds_graphcon, mean)
ds_graphinc <- ds_graph %>% filter(ds_graph$congruent=="no") %>% rename(noRT=RT)
incongruent_rts <- aggregate(noRT ~ subjectID, ds_graphinc, mean)
congruent_rts
incongruent_rts
ds_graph <- ds %>% filter(ds$presentation.time==1700)
ds_graph <- select(ds_graph, one_of(c("subjectID","presentation.time","congruent","RT")))
ds_graphcon <- ds_graph %>% filter(ds_graph$congruent=="yes") %>% rename(yesRT=RT)
congruent_rts <- aggregate(yesRT ~ subjectID, ds_graphcon, mean)
ds_graphinc <- ds_graph %>% filter(ds_graph$congruent=="no") %>% rename(noRT=RT)
incongruent_rts <- aggregate(noRT ~ subjectID, ds_graphinc, mean)
congruent_rts
incongruent_rts
ds_1700 <- left_join(congruent_rts, incongruent_rts, by="subjectID")
ds_1700
ds_graph <- ds %>% filter(ds$presentation.time==1700)
ds_graph <- select(ds_graph, one_of(c("subjectID","presentation.time","congruent","RT")))
ds_graphcon <- ds_graph %>% filter(ds_graph$congruent=="yes") %>% rename(yesRT=RT)
congruent_rts <- aggregate(yesRT ~ subjectID, ds_graphcon, mean)
ds_graphinc <- ds_graph %>% filter(ds_graph$congruent=="no") %>% rename(noRT=RT)
incongruent_rts <- aggregate(noRT ~ subjectID, ds_graphinc, mean)
ds_1700 <- left_join(congruent_rts, incongruent_rts, by="subjectID") %>% mutate(diff = noRT-yesRT)
ds_graph <- ds %>% filter(ds$presentation.time==2000)
ds_graph <- select(ds_graph, one_of(c("subjectID","presentation.time","congruent","RT")))
ds_graphcon <- ds_graph %>% filter(ds_graph$congruent=="yes") %>% rename(yesRT=RT)
congruent_rts <- aggregate(yesRT ~ subjectID, ds_graphcon, mean)
ds_graphinc <- ds_graph %>% filter(ds_graph$congruent=="no") %>% rename(noRT=RT)
incongruent_rts <- aggregate(noRT ~ subjectID, ds_graphinc, mean)
ds_2000 <- left_join(congruent_rts, incongruent_rts, by="subjectID") %>% mutate(diff = noRT-yesRT)
ds_1700
ds <- d.all.tidy %>% filter((d.all.tidy$subjective.test==0) & (d.all.tidy$objective.test < .6))
ds <- tbl_df(ds)
plot.style <- theme(panel.grid.major=element_blank(), legend.position="right", plot.title=element_text(face="bold", size=14), axis.title=element_text(size=12))
PD <- position_dodge(.9)
ds_graph <- ds %>% filter(ds$presentation.time==1700)
ds_graph <- select(ds_graph, one_of(c("subjectID","presentation.time","congruent","RT")))
ds_graphcon <- ds_graph %>% filter(ds_graph$congruent=="yes") %>% rename(yesRT=RT)
congruent_rts <- aggregate(yesRT ~ subjectID, ds_graphcon, mean)
ds_graphinc <- ds_graph %>% filter(ds_graph$congruent=="no") %>% rename(noRT=RT)
incongruent_rts <- aggregate(noRT ~ subjectID, ds_graphinc, mean)
ds_1700 <- left_join(congruent_rts, incongruent_rts, by="subjectID") %>% mutate(diff = noRT-yesRT)
ds_graph <- ds %>% filter(ds$presentation.time==2000)
ds_graph <- select(ds_graph, one_of(c("subjectID","presentation.time","congruent","RT")))
ds_graphcon <- ds_graph %>% filter(ds_graph$congruent=="yes") %>% rename(yesRT=RT)
congruent_rts <- aggregate(yesRT ~ subjectID, ds_graphcon, mean)
ds_graphinc <- ds_graph %>% filter(ds_graph$congruent=="no") %>% rename(noRT=RT)
incongruent_rts <- aggregate(noRT ~ subjectID, ds_graphinc, mean)
ds_2000 <- left_join(congruent_rts, incongruent_rts, by="subjectID") %>% mutate(diff = noRT-yesRT)
ds_1700
ds_2000
rts1700.SEM <- aggregate(diff ~ presentation.time, ds_1700, sem)
ds_1700$sem <- rts1700.SEM[,2]
ds_1700 <- left_join(ds_1700, ds_graph, by="subjectID")
ds_1700
ds_graph <- ds %>% filter(ds$presentation.time==1700)
ds_graph
ds_graph <- select(ds_graph, one_of(c("subjectID","presentation.time","congruent","RT")))
ds_graph
View(ds_graph)
ds_graphcon <- ds_graph %>% filter(ds_graph$congruent=="yes") %>% rename(yesRT=RT)
ds_1700 <- ds_1700 %>% mutate(ds_1700$presentation.time = 1700)
ds_1700 <- ds_1700 %>% mutate(presentation.time = 1700)
ds_1700
ds_graph <- ds %>% filter(ds$presentation.time==1700)
ds_graph <- select(ds_graph, one_of(c("subjectID","presentation.time","congruent","RT")))
ds_graphcon <- ds_graph %>% filter(ds_graph$congruent=="yes") %>% rename(yesRT=RT)
congruent_rts <- aggregate(yesRT ~ subjectID, ds_graphcon, mean)
ds_graphinc <- ds_graph %>% filter(ds_graph$congruent=="no") %>% rename(noRT=RT)
incongruent_rts <- aggregate(noRT ~ subjectID, ds_graphinc, mean)
ds_1700 <- left_join(congruent_rts, incongruent_rts, by="subjectID") %>% mutate(diff = noRT-yesRT)
ds_1700 <- ds_1700 %>% mutate(presentation.time = 1700)
rts1700.SEM <- aggregate(diff ~ presentation.time, ds_1700, sem)
ds_1700$sem <- rts1700.SEM[,2]
ds_1700
ds_graph <- ds %>% filter(ds$presentation.time==2000)
ds_graph <- select(ds_graph, one_of(c("subjectID","presentation.time","congruent","RT")))
ds_graphcon <- ds_graph %>% filter(ds_graph$congruent=="yes") %>% rename(yesRT=RT)
congruent_rts <- aggregate(yesRT ~ subjectID, ds_graphcon, mean)
ds_graphinc <- ds_graph %>% filter(ds_graph$congruent=="no") %>% rename(noRT=RT)
incongruent_rts <- aggregate(noRT ~ subjectID, ds_graphinc, mean)
ds_2000 <- left_join(congruent_rts, incongruent_rts, by="subjectID") %>% mutate(diff = noRT-yesRT)
ds_2000 <- ds_2000 %>% mutate(presentation.time = 2000)
rts2000.SEM <- aggregate(diff ~ presentation.time, ds_2000, sem)
ds_2000$sem <- rts2000.SEM[,2]
ds_2000
ds_graph <- ds %>% filter(ds$presentation.time==1700)
ds_graph <- select(ds_graph, one_of(c("subjectID","presentation.time","congruent","RT")))
ds_graphcon <- ds_graph %>% filter(ds_graph$congruent=="yes") %>% rename(yesRT=RT)
congruent_rts <- aggregate(yesRT ~ subjectID, ds_graphcon, mean)
ds_graphinc <- ds_graph %>% filter(ds_graph$congruent=="no") %>% rename(noRT=RT)
incongruent_rts <- aggregate(noRT ~ subjectID, ds_graphinc, mean)
ds_1700 <- left_join(congruent_rts, incongruent_rts, by="subjectID") %>% mutate(diff = noRT-yesRT)
ds_1700 <- ds_1700 %>% mutate(presentation.time = 1700)
rts1700.SEM <- aggregate(diff ~ presentation.time, ds_1700, sem)
ds_1700$sem <- rts1700.SEM[,2]
ds_1700 <- ds_1700 %>% mutate(meanDiff = mean(diff))
ds_graph <- ds %>% filter(ds$presentation.time==2000)
ds_graph <- select(ds_graph, one_of(c("subjectID","presentation.time","congruent","RT")))
ds_graphcon <- ds_graph %>% filter(ds_graph$congruent=="yes") %>% rename(yesRT=RT)
congruent_rts <- aggregate(yesRT ~ subjectID, ds_graphcon, mean)
ds_graphinc <- ds_graph %>% filter(ds_graph$congruent=="no") %>% rename(noRT=RT)
incongruent_rts <- aggregate(noRT ~ subjectID, ds_graphinc, mean)
ds_2000 <- left_join(congruent_rts, incongruent_rts, by="subjectID") %>% mutate(diff = noRT-yesRT)
ds_2000 <- ds_2000 %>% mutate(presentation.time = 2000)
rts2000.SEM <- aggregate(diff ~ presentation.time, ds_2000, sem)
ds_2000$sem <- rts2000.SEM[,2]
ds_2000 <- ds_2000 %>% mutate(meanDiff = mean(diff))
ds_final <- bind_rows(ds_1700,ds_2000)
ds_final
plot.style <- theme(panel.grid.major=element_blank(), legend.position="right", plot.title=element_text(face="bold", size=14), axis.title=element_text(size=12))
PD <- position_dodge(.9)
ds_graph <- ds %>% filter(ds$presentation.time==1700)
ds_graph <- select(ds_graph, one_of(c("subjectID","presentation.time","congruent","RT")))
ds_graphcon <- ds_graph %>% filter(ds_graph$congruent=="yes") %>% rename(yesRT=RT)
congruent_rts <- aggregate(yesRT ~ subjectID, ds_graphcon, mean)
ds_graphinc <- ds_graph %>% filter(ds_graph$congruent=="no") %>% rename(noRT=RT)
incongruent_rts <- aggregate(noRT ~ subjectID, ds_graphinc, mean)
ds_1700 <- left_join(congruent_rts, incongruent_rts, by="subjectID") %>% mutate(diff = noRT-yesRT)
ds_1700 <- ds_1700 %>% mutate(presentation.time = 1700)
ds_1700 <- ds_1700 %>% mutate(meanDiff = mean(diff))
ds_graph <- ds %>% filter(ds$presentation.time==2000)
ds_graph <- select(ds_graph, one_of(c("subjectID","presentation.time","congruent","RT")))
ds_graphcon <- ds_graph %>% filter(ds_graph$congruent=="yes") %>% rename(yesRT=RT)
congruent_rts <- aggregate(yesRT ~ subjectID, ds_graphcon, mean)
ds_graphinc <- ds_graph %>% filter(ds_graph$congruent=="no") %>% rename(noRT=RT)
incongruent_rts <- aggregate(noRT ~ subjectID, ds_graphinc, mean)
ds_2000 <- left_join(congruent_rts, incongruent_rts, by="subjectID") %>% mutate(diff = noRT-yesRT)
ds_2000 <- ds_2000 %>% mutate(presentation.time = 2000) %>% mutate(meanDiff = mean(diff))
ds_final <- bind_rows(ds_1700,ds_2000)
ds.SEM <- aggregate(meanDiff ~ presentation.time, ds_final, sem)
ds_final$sem <- ds.SEM[,2]
ds.SEM
ds_final
ds_final <- bind_rows(ds_1700,ds_2000)
ds_final <- ds_final %>% mutate(sem = sem(meanDiff))
ds_final
glimpse(ds_final)
qplot(presentation.time, meanDiff,
geom="bar",
position="dodge",
stat="identity", fill="green",
data=ds_final) +
geom_errorbar(aes(ymin=diff-sem, ymax=diff+sem), colour="red", width=.2, position=PD) +
xlab("Representation Duration") +
ylab("Facilitation (mean)") +
ggtitle("Facilitation Effect") +
plot.style
qplot(presentation.time, meanDiff,
geom="bar",
position="dodge",
stat="identity",
data=ds_final) +
geom_errorbar(aes(ymin=diff-sem, ymax=diff+sem), colour="red", width=.2, position=PD) +
xlab("Representation Duration") +
ylab("Facilitation (mean)") +
ggtitle("Facilitation Effect")
ds_final$sem
qplot(presentation.time, meanDiff,
geom="bar",
position="dodge",
stat="identity",
data=ds_final) +
geom_errorbar(aes(ymin=diff-sem, ymax=diff+sem), colour="black", width=.2, position=PD) +
xlab("Representation Duration") +
ylab("Facilitation (mean)") +
ggtitle("Facilitation Effect")
qplot(presentation.time, meanDiff,
geom="bar",
position="dodge",
stat="identity",
data=ds_final) +
geom_errorbar(aes(ymin=diff-sem, ymax=diff+sem), colour="black", width=.2, position=PD) +
xlab("Representation Duration") +
ylab("Facilitation (mean)") +
ggtitle("Facilitation Effect") + theme_bw()
ds_final
?abs()
ds_graph <- ds %>% filter(ds$presentation.time==1700)
ds_graph <- select(ds_graph, one_of(c("subjectID","presentation.time","congruent","RT")))
ds_graphcon <- ds_graph %>% filter(ds_graph$congruent=="yes") %>% rename(yesRT=RT)
congruent_rts <- aggregate(yesRT ~ subjectID, ds_graphcon, mean)
ds_graphinc <- ds_graph %>% filter(ds_graph$congruent=="no") %>% rename(noRT=RT)
incongruent_rts <- aggregate(noRT ~ subjectID, ds_graphinc, mean)
ds_1700 <- left_join(congruent_rts, incongruent_rts, by="subjectID") %>% mutate(diff = abs(noRT-yesRT))
ds_1700 <- ds_1700 %>% mutate(presentation.time = 1700)
ds_1700 <- ds_1700 %>% mutate(meanDiff = mean(diff))
ds_graph <- ds %>% filter(ds$presentation.time==2000)
ds_graph <- select(ds_graph, one_of(c("subjectID","presentation.time","congruent","RT")))
ds_graphcon <- ds_graph %>% filter(ds_graph$congruent=="yes") %>% rename(yesRT=RT)
congruent_rts <- aggregate(yesRT ~ subjectID, ds_graphcon, mean)
ds_graphinc <- ds_graph %>% filter(ds_graph$congruent=="no") %>% rename(noRT=RT)
incongruent_rts <- aggregate(noRT ~ subjectID, ds_graphinc, mean)
ds_2000 <- left_join(congruent_rts, incongruent_rts, by="subjectID") %>% mutate(diff = abs(noRT-yesRT))
ds_2000 <- ds_2000 %>% mutate(presentation.time = 2000) %>% mutate(meanDiff = mean(diff))
ds_final <- bind_rows(ds_1700,ds_2000)
ds_final <- ds_final %>% mutate(sem = sem(meanDiff))
sem = sem(meanDiff)
qplot(presentation.time, meanDiff,
geom="bar",
position="dodge",
stat="identity",
data=ds_final) +
geom_errorbar(aes(ymin=diff-sem, ymax=diff+sem), colour="black", width=.2, position=PD) +
xlab("Representation Duration") +
ylab("Facilitation (mean)") +
ggtitle("Facilitation Effect") + theme_bw()
ds_final$presentation.time <- as.factor(ds_final$presentation.time)
qplot(presentation.time, meanDiff,
geom="bar",
position="dodge",
stat="identity",
data=ds_final) +
geom_errorbar(aes(ymin=diff-sem, ymax=diff+sem), colour="black", width=.2, position=PD) +
xlab("Representation Duration") +
ylab("Facilitation (mean)") +
ggtitle("Facilitation Effect") + theme_bw()
ds_graph <- ds
glimpse(ds_graph)
ds_graph <- ds %>% filter(ds$presentation.time==1700)
ds_graph <- select(ds_graph, one_of(c("subjectID","presentation.time","congruent","RT")))
ds_graphcon <- ds_graph %>% filter(ds_graph$congruent=="yes") %>% rename(yesRT=RT)
congruent_rts <- aggregate(yesRT ~ subjectID, ds_graphcon, mean)
ds_graphinc <- ds_graph %>% filter(ds_graph$congruent=="no") %>% rename(noRT=RT)
incongruent_rts <- aggregate(noRT ~ subjectID, ds_graphinc, mean)
ds_1700 <- left_join(congruent_rts, incongruent_rts, by="subjectID") %>% mutate(diff = abs(noRT-yesRT))
ds_1700 <- ds_1700 %>% mutate(presentation.time = 1700)
ds_1700 <- ds_1700 %>% mutate(meanDiff = mean(diff))
ds_graph <- ds %>% filter(ds$presentation.time==2000)
ds_graph <- select(ds_graph, one_of(c("subjectID","presentation.time","congruent","RT")))
ds_graphcon <- ds_graph %>% filter(ds_graph$congruent=="yes") %>% rename(yesRT=RT)
congruent_rts <- aggregate(yesRT ~ subjectID, ds_graphcon, mean)
ds_graphinc <- ds_graph %>% filter(ds_graph$congruent=="no") %>% rename(noRT=RT)
incongruent_rts <- aggregate(noRT ~ subjectID, ds_graphinc, mean)
ds_2000 <- left_join(congruent_rts, incongruent_rts, by="subjectID") %>% mutate(diff = abs(noRT-yesRT))
ds_2000 <- ds_2000 %>% mutate(presentation.time = 2000) %>% mutate(meanDiff = mean(diff))
ds_final <- bind_rows(ds_1700,ds_2000)
ds_final <- ds_final %>% mutate(sem = sem(meanDiff))
sem = sem(meanDiff)
ds_final$presentation.time <- as.factor(ds_final$presentation.time)
ds_graph <- ds %>% filter(ds$presentation.time==1700)
ds_graph <- select(ds_graph, one_of(c("subjectID","presentation.time","congruent","RT")))
ds_graphcon <- ds_graph %>% filter(ds_graph$congruent=="yes") %>% rename(yesRT=RT)
congruent_rts <- aggregate(yesRT ~ subjectID, ds_graphcon, mean)
ds_graphinc <- ds_graph %>% filter(ds_graph$congruent=="no") %>% rename(noRT=RT)
incongruent_rts <- aggregate(noRT ~ subjectID, ds_graphinc, mean)
ds_1700 <- left_join(congruent_rts, incongruent_rts, by="subjectID") %>% mutate(diff = abs(noRT-yesRT))
ds_1700 <- ds_1700 %>% mutate(presentation.time = 1700)
ds_1700 <- ds_1700 %>% mutate(meanDiff = mean(diff))
ds_graph <- ds %>% filter(ds$presentation.time==2000)
ds_graph <- select(ds_graph, one_of(c("subjectID","presentation.time","congruent","RT")))
ds_graphcon <- ds_graph %>% filter(ds_graph$congruent=="yes") %>% rename(yesRT=RT)
congruent_rts <- aggregate(yesRT ~ subjectID, ds_graphcon, mean)
ds_graphinc <- ds_graph %>% filter(ds_graph$congruent=="no") %>% rename(noRT=RT)
incongruent_rts <- aggregate(noRT ~ subjectID, ds_graphinc, mean)
ds_2000 <- left_join(congruent_rts, incongruent_rts, by="subjectID") %>% mutate(diff = abs(noRT-yesRT))
ds_2000 <- ds_2000 %>% mutate(presentation.time = 2000) %>% mutate(meanDiff = mean(diff))
ds_final <- bind_rows(ds_1700,ds_2000)
ds_final <- ds_final %>% mutate(sem = sem(meanDiff))
sem = sem(meanDiff)
ds_final$presentation.time <- as.factor(ds_final$presentation.time)
plot.style <- theme(panel.grid.major=element_blank(), legend.position="right", plot.title=element_text(face="bold", size=14), axis.title=element_text(size=12))
PD <- position_dodge(.9)
ds_graph <- ds %>% filter(ds$presentation.time==1700)
ds_graph <- select(ds_graph, one_of(c("subjectID","presentation.time","congruent","RT")))
ds_graphcon <- ds_graph %>% filter(ds_graph$congruent=="yes") %>% rename(yesRT=RT)
congruent_rts <- aggregate(yesRT ~ subjectID, ds_graphcon, mean)
ds_graphinc <- ds_graph %>% filter(ds_graph$congruent=="no") %>% rename(noRT=RT)
incongruent_rts <- aggregate(noRT ~ subjectID, ds_graphinc, mean)
ds_1700 <- left_join(congruent_rts, incongruent_rts, by="subjectID") %>% mutate(diff = abs(noRT-yesRT))
ds_1700 <- ds_1700 %>% mutate(presentation.time = 1700)
ds_1700 <- ds_1700 %>% mutate(meanDiff = mean(diff))
ds_graph <- ds %>% filter(ds$presentation.time==2000)
ds_graph <- select(ds_graph, one_of(c("subjectID","presentation.time","congruent","RT")))
ds_graphcon <- ds_graph %>% filter(ds_graph$congruent=="yes") %>% rename(yesRT=RT)
congruent_rts <- aggregate(yesRT ~ subjectID, ds_graphcon, mean)
ds_graphinc <- ds_graph %>% filter(ds_graph$congruent=="no") %>% rename(noRT=RT)
incongruent_rts <- aggregate(noRT ~ subjectID, ds_graphinc, mean)
ds_2000 <- left_join(congruent_rts, incongruent_rts, by="subjectID") %>% mutate(diff = abs(noRT-yesRT))
ds_2000 <- ds_2000 %>% mutate(presentation.time = 2000) %>% mutate(meanDiff = mean(diff))
ds_final <- bind_rows(ds_1700,ds_2000)
ds_final <- ds_final %>% mutate(sem = sem(meanDiff))
glimpse(ds_final)
plot.style <- theme(panel.grid.major=element_blank(), legend.position="right", plot.title=element_text(face="bold", size=14), axis.title=element_text(size=12))
PD <- position_dodge(.9)
ds_graph <- ds %>% filter(ds$presentation.time==1700)
ds_graph <- select(ds_graph, one_of(c("subjectID","presentation.time","congruent","RT")))
ds_graphcon <- ds_graph %>% filter(ds_graph$congruent=="yes") %>% rename(yesRT=RT)
congruent_rts <- aggregate(yesRT ~ subjectID, ds_graphcon, mean)
ds_graphinc <- ds_graph %>% filter(ds_graph$congruent=="no") %>% rename(noRT=RT)
incongruent_rts <- aggregate(noRT ~ subjectID, ds_graphinc, mean)
ds_1700 <- left_join(congruent_rts, incongruent_rts, by="subjectID") %>% mutate(diff = abs(noRT-yesRT))
ds_1700 <- ds_1700 %>% mutate(presentation.time = 1700)
ds_1700 <- ds_1700 %>% mutate(meanDiff = mean(diff))
ds_graph <- ds %>% filter(ds$presentation.time==2000)
ds_graph <- select(ds_graph, one_of(c("subjectID","presentation.time","congruent","RT")))
ds_graphcon <- ds_graph %>% filter(ds_graph$congruent=="yes") %>% rename(yesRT=RT)
congruent_rts <- aggregate(yesRT ~ subjectID, ds_graphcon, mean)
ds_graphinc <- ds_graph %>% filter(ds_graph$congruent=="no") %>% rename(noRT=RT)
incongruent_rts <- aggregate(noRT ~ subjectID, ds_graphinc, mean)
ds_2000 <- left_join(congruent_rts, incongruent_rts, by="subjectID") %>% mutate(diff = abs(noRT-yesRT))
ds_2000 <- ds_2000 %>% mutate(presentation.time = 2000) %>% mutate(meanDiff = mean(diff))
ds_final <- bind_rows(ds_1700,ds_2000)
ds_final <- ds_final %>% mutate(sem = sem(meanDiff))
qplot(presentation.time, meanDiff,
geom="bar",
position="dodge",
stat="identity",
data=ds_final) +
geom_errorbar(aes(ymin=diff-sem, ymax=diff+sem), colour="black", width=.2, position=PD) +
xlab("Representation Duration") +
ylab("Facilitation (mean)") +
ggtitle("Facilitation Effect") + theme_bw()
plot.style <- theme(panel.grid.major=element_blank(), legend.position="right", plot.title=element_text(face="bold", size=14), axis.title=element_text(size=12))
PD <- position_dodge(.9)
ds_graph <- ds %>% filter(ds$presentation.time==1700)
ds_graph <- select(ds_graph, one_of(c("subjectID","presentation.time","congruent","RT")))
ds_graphcon <- ds_graph %>% filter(ds_graph$congruent=="yes") %>% rename(yesRT=RT)
congruent_rts <- aggregate(yesRT ~ subjectID, ds_graphcon, mean)
ds_graphinc <- ds_graph %>% filter(ds_graph$congruent=="no") %>% rename(noRT=RT)
incongruent_rts <- aggregate(noRT ~ subjectID, ds_graphinc, mean)
ds_1700 <- left_join(congruent_rts, incongruent_rts, by="subjectID") %>% mutate(diff = abs(noRT-yesRT))
ds_1700 <- ds_1700 %>% mutate(presentation.time = 1700)
ds_1700 <- ds_1700 %>% mutate(meanDiff = mean(diff))
ds_graph <- ds %>% filter(ds$presentation.time==2000)
ds_graph <- select(ds_graph, one_of(c("subjectID","presentation.time","congruent","RT")))
ds_graphcon <- ds_graph %>% filter(ds_graph$congruent=="yes") %>% rename(yesRT=RT)
congruent_rts <- aggregate(yesRT ~ subjectID, ds_graphcon, mean)
ds_graphinc <- ds_graph %>% filter(ds_graph$congruent=="no") %>% rename(noRT=RT)
incongruent_rts <- aggregate(noRT ~ subjectID, ds_graphinc, mean)
ds_2000 <- left_join(congruent_rts, incongruent_rts, by="subjectID") %>% mutate(diff = abs(noRT-yesRT))
ds_2000 <- ds_2000 %>% mutate(presentation.time = 2000) %>% mutate(meanDiff = mean(diff))
ds_final <- bind_rows(ds_1700,ds_2000)
ds_final <- ds_final %>% mutate(sem = sem(meanDiff))
qplot(presentation.time, meanDiff,
geom="bar",
position="dodge",
stat="identity",
data=ds_final) +
geom_errorbar(aes(ymin=diff-sem, ymax=diff+sem), colour="black", width=.2, position=PD) +
xlab("Representation Duration") +
ylab("Facilitation (mean)") +
ggtitle("Facilitation Effect") + plot.style
qplot(presentation.time, meanDiff,
geom="bar",
position="dodge",
stat="identity",
data=ds_final) +
geom_errorbar(aes(ymin=diff-sem, ymax=diff+sem), colour="black", width=.2, position=PD) +
xlab("Representation Duration") +
ylab("Facilitation (mean)") +
ggtitle("Facilitation Effect") + plot.style
plot.style <- theme(panel.grid.major=element_blank(), legend.position="right", plot.title=element_text(face="bold", size=14), axis.title=element_text(size=12))
PD <- position_dodge(.9)
ds_graph <- ds %>% filter(ds$presentation.time==1700)
ds_graph <- select(ds_graph, one_of(c("subjectID","presentation.time","congruent","RT")))
ds_graphcon <- ds_graph %>% filter(ds_graph$congruent=="yes") %>% rename(yesRT=RT)
congruent_rts <- aggregate(yesRT ~ subjectID, ds_graphcon, mean)
ds_graphinc <- ds_graph %>% filter(ds_graph$congruent=="no") %>% rename(noRT=RT)
incongruent_rts <- aggregate(noRT ~ subjectID, ds_graphinc, mean)
ds_1700 <- left_join(congruent_rts, incongruent_rts, by="subjectID") %>% mutate(diff = noRT-yesRT)
ds_1700 <- ds_1700 %>% mutate(presentation.time = 1700)
ds_1700 <- ds_1700 %>% mutate(meanDiff = mean(diff))
ds_graph <- ds %>% filter(ds$presentation.time==2000)
ds_graph <- select(ds_graph, one_of(c("subjectID","presentation.time","congruent","RT")))
ds_graphcon <- ds_graph %>% filter(ds_graph$congruent=="yes") %>% rename(yesRT=RT)
congruent_rts <- aggregate(yesRT ~ subjectID, ds_graphcon, mean)
ds_graphinc <- ds_graph %>% filter(ds_graph$congruent=="no") %>% rename(noRT=RT)
incongruent_rts <- aggregate(noRT ~ subjectID, ds_graphinc, mean)
ds_2000 <- left_join(congruent_rts, incongruent_rts, by="subjectID") %>% mutate(diff = noRT-yesRT)
ds_2000 <- ds_2000 %>% mutate(presentation.time = 2000) %>% mutate(meanDiff = mean(diff))
ds_final <- bind_rows(ds_1700,ds_2000)
ds_final <- ds_final %>% mutate(sem = sem(meanDiff))
qplot(presentation.time, meanDiff,
geom="bar",
position="dodge",
stat="identity",
data=ds_final) +
geom_errorbar(aes(ymin=diff-sem, ymax=diff+sem), colour="black", width=.2, position=PD) +
xlab("Representation Duration") +
ylab("Facilitation (mean)") +
ggtitle("Facilitation Effect") + plot.style
plot.style <- theme(panel.grid.major=element_blank(), legend.position="right", plot.title=element_text(face="bold", size=14), axis.title=element_text(size=12))
PD <- position_dodge(.9)
ds_graph <- ds %>% filter(ds$presentation.time==1700)
ds_graph <- select(ds_graph, one_of(c("subjectID","presentation.time","congruent","RT")))
ds_graphcon <- ds_graph %>% filter(ds_graph$congruent=="yes") %>% rename(yesRT=RT)
congruent_rts <- aggregate(yesRT ~ subjectID, ds_graphcon, mean)
ds_graphinc <- ds_graph %>% filter(ds_graph$congruent=="no") %>% rename(noRT=RT)
incongruent_rts <- aggregate(noRT ~ subjectID, ds_graphinc, mean)
ds_1700 <- left_join(congruent_rts, incongruent_rts, by="subjectID") %>% mutate(diff = abs(noRT-yesRT))
ds_1700 <- ds_1700 %>% mutate(presentation.time = 1700)
ds_1700 <- ds_1700 %>% mutate(meanDiff = mean(diff))
ds_graph <- ds %>% filter(ds$presentation.time==2000)
ds_graph <- select(ds_graph, one_of(c("subjectID","presentation.time","congruent","RT")))
ds_graphcon <- ds_graph %>% filter(ds_graph$congruent=="yes") %>% rename(yesRT=RT)
congruent_rts <- aggregate(yesRT ~ subjectID, ds_graphcon, mean)
ds_graphinc <- ds_graph %>% filter(ds_graph$congruent=="no") %>% rename(noRT=RT)
incongruent_rts <- aggregate(noRT ~ subjectID, ds_graphinc, mean)
ds_2000 <- left_join(congruent_rts, incongruent_rts, by="subjectID") %>% mutate(diff = abs(noRT-yesRT))
ds_2000 <- ds_2000 %>% mutate(presentation.time = 2000) %>% mutate(meanDiff = mean(diff))
ds_final <- bind_rows(ds_1700,ds_2000)
ds_final <- ds_final %>% mutate(sem = sem(meanDiff))
qplot(presentation.time, meanDiff,
geom="bar",
position="dodge",
stat="identity",
data=ds_final) +
geom_errorbar(aes(ymin=diff-sem, ymax=diff+sem), colour="black", width=.2, position=PD) +
xlab("Representation Duration") +
ylab("Facilitation (mean)") +
ggtitle("Facilitation Effect") + plot.style
ds
plot.style <- theme(panel.grid.major=element_blank(), legend.position="right", plot.title=element_text(face="bold", size=14), axis.title=element_text(size=12))
PD <- position_dodge(.9)
ds_graph <- ds %>% filter((ds$presentation.time==1700) & (ds$operand=="subtraction"))
ds_graph <- select(ds_graph, one_of(c("subjectID","presentation.time","congruent","RT")))
ds_graphcon <- ds_graph %>% filter(ds_graph$congruent=="yes") %>% rename(yesRT=RT)
congruent_rts <- aggregate(yesRT ~ subjectID, ds_graphcon, mean)
ds_graphinc <- ds_graph %>% filter(ds_graph$congruent=="no") %>% rename(noRT=RT)
incongruent_rts <- aggregate(noRT ~ subjectID, ds_graphinc, mean)
ds_1700 <- left_join(congruent_rts, incongruent_rts, by="subjectID") %>% mutate(diff = abs(noRT-yesRT))
ds_1700 <- ds_1700 %>% mutate(presentation.time = 1700)
ds_1700 <- ds_1700 %>% mutate(meanDiff = mean(diff))
ds_graph <- ds %>% filter(ds$presentation.time==2000)
ds_graph <- select(ds_graph, one_of(c("subjectID","presentation.time","congruent","RT")))
ds_graphcon <- ds_graph %>% filter(ds_graph$congruent=="yes") %>% rename(yesRT=RT)
congruent_rts <- aggregate(yesRT ~ subjectID, ds_graphcon, mean)
ds_graphinc <- ds_graph %>% filter(ds_graph$congruent=="no") %>% rename(noRT=RT)
incongruent_rts <- aggregate(noRT ~ subjectID, ds_graphinc, mean)
ds_2000 <- left_join(congruent_rts, incongruent_rts, by="subjectID") %>% mutate(diff = abs(noRT-yesRT))
ds_2000 <- ds_2000 %>% mutate(presentation.time = 2000) %>% mutate(meanDiff = mean(diff))
ds_final <- bind_rows(ds_1700,ds_2000)
ds_final <- ds_final %>% mutate(sem = sem(meanDiff))
qplot(presentation.time, meanDiff,
geom="bar",
position="dodge",
stat="identity",
data=ds_final) +
geom_errorbar(aes(ymin=diff-sem, ymax=diff+sem), colour="black", width=.2, position=PD) +
xlab("Representation Duration") +
ylab("Facilitation (mean)") +
ggtitle("Facilitation Effect") + plot.style
qplot(presentation.time, meanDiff,
geom="bar",
position="dodge",
stat="identity",
data=ds_final) +
geom_linerange(aes(ymin=diff-sem, ymax=diff+sem), colour="black", width=.2, position=PD) +
xlab("Representation Duration") +
ylab("Facilitation (mean)") +
ggtitle("Facilitation Effect") + plot.style
ds
glimpse(d.all.tidy)
ggplot(d.all.tidy, aes(x=RT, y=obbjective.test)) + geom_point() +
stat_smooth(method="lm", se=FALSE)
ggplot(d.all.tidy, aes(x=RT, y=objective.test)) + geom_point() +
stat_smooth(method="lm", se=FALSE)
ggplot(ds, aes(x=RT, y=objective.test)) + geom_point() +
stat_smooth(method="lm", se=FALSE)
manipcheckcheck <- glm(objective.test ~ RT, ds, na.action = na.omit)
print(summary(manipcheckcheck))
manipcheckcheck <- lm(objective.test ~ RT, ds, na.action = na.omit)
print(summary(manipcheckcheck))
assessment <- glm(congruent ~ RT, family=binomial, d.all.tidy, na.action = na.omit)
print(summary(assessment))
ggplot(d.all.tidy, aes(x=RT, y=congruent)) + geom_point() +
stat_smooth(method="glm", family="binomial", se=FALSE)
assessment2 <- lm(congruent ~ RT, family=binomial, d.all.tidy, na.action = na.omit)
print(summary(assessment2))
ggplot(d.all.tidy, aes(x=RT, y=congruent)) + geom_point() +
facets = (. ~ operand) +
stat_smooth(method="lm", se=FALSE)
assessment2 <- lm(congruent ~ RT, family=binomial, d.all.tidy, na.action = na.omit)
print(summary(assessment2))
assessment2 <- glm(operant ~ RT, family=binomial, d.all.tidy, na.action = na.omit)
print(summary(assessment2))
assessment <- glm(congruent ~ RT, family=binomial, d.all.tidy, na.action = na.omit)
print(summary(assessment))
ggplot(d.all.tidy, aes(x=RT, y=congruent)) + geom_point() +
stat_smooth(method="glm", family="binomial", se=FALSE)
assessment2 <- glm(operant ~ RT, family=binomial, d.all.tidy, na.action = na.omit)
print(summary(assessment2))
assessment2 <- glm(operand ~ RT, family=binomial, d.all.tidy, na.action = na.omit)
print(summary(assessment2))
